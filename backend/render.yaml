"""
Render deployment configuration for DakDash Backend
"""

[build]
# Build command
command = "pip install -r requirements.txt"

[deploy]
# Start command
command = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[env]
# Python version
PYTHON_VERSION = "3.11"
